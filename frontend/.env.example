# =============================================================================
# NAS Cloud Frontend — Environment Variables (.env.example)
# Copy to .env.local and set VITE_API_BASE_URL to your backend HTTPS address.
# =============================================================================

# Backend API base URL (must be HTTPS). Leave empty to use Vite proxy (/api).
VITE_API_BASE_URL=
